language: go

go:
  - "1.12.x"

env:
  - GO111MODULE=on

branches:
  only:
    - master

install: skip

before_script:
  - GO111MODULE=off go get golang.org/x/lint

script:
  - cd chaincode
  - go test -v ./...
  - go build
  - golint
