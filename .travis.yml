language: go

go:
  - "1.12.x"

env:
  - GO111MODULE=on

branches:
  only:
    - master

install: skip

before_script:
  - go get golang.org/x/lint/golint

script:
  - cd chaincode
  - go test -v ./...
  - go build -v
  - go list ./... | grep -v /vendor/ | xargs -L1 golint -set_exit_status

