language: go

go:
  - "1.12.x"

env:
  - GO111MODULE=on

branches:
  only:
    - master

install: skip

before_script:
  - go get -x github.com/golang/lint/golint

script:
  - golint
  - cd chaincode
  - go test -v ./...
  - go build
  
