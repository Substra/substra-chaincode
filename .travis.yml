language: go

go:
  - "1.12.x"

env:
  - GO111MODULE=on

branches:
  only:
    - master
    
install: true

before_install:
  - go install golang.org/x/lint/golint

script:
  - cd chaincode
  - go test -v ./...
  - go build
  - test -z "`golint ./...`"
