language: go

go:
  - "1.11.x"

env:
  - GO111MODULE=on

branches:
  only:
    - master

install: skip

before_script:
  - go get golang.org/x/lint/golint

script:
  - cd chaincode
  - golint
  - go test -v ./...
  - go build -v

