apiVersion: v1
kind: Service
metadata:
  name: {{ include "substra-chaincode.fullname" . }}
  labels:
    {{- include "substra-chaincode.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "substra-chaincode.selectorLabels" . | nindent 4 }}
